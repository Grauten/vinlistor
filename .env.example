# Copy to .env and fill in. .env is gitignored — never commit real keys.

# Claude API (same key style as vinappen). Used to parse messy menus into JSON.
ANTHROPIC_API_KEY=sk-ant-...

# Supabase — project ref ybyynrlfqbbjkybgldrm (eu-west-1).
# Find these in Supabase dashboard → Project Settings → API.
SUPABASE_URL=https://ybyynrlfqbbjkybgldrm.supabase.co
# Use the SERVICE ROLE key here (server-side script, bypasses RLS). Keep secret.
SUPABASE_SERVICE_ROLE_KEY=ey...
